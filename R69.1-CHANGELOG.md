# TOR POS R69.1 – Migration Compile Hotfix

## Derleme hatası
R69 build logunda iki aynı hata vardı:

`SchemaMigrationService.cs(266,57): CS0234`
`SchemaMigrationService.cs(288,57): CS0234`

Kod yanlışlıkla:
`TorPos.Core.ReleaseInfo.Version`

kullanıyordu.

Gerçek release sınıfı:
`TorPos.Core.TorRelease`

olduğu için iki referans şu şekilde düzeltildi:
`TorPos.Core.TorRelease.Version`

## Test hedefi düzeltmesi
R69ReviewTests içinde fiilen:
- 6 `assert`
- 2 `reject`

olmak üzere 8 yeni kontrol var.

R68.1 hedefi 254 olduğundan R69.1 doğru hedef:
`ALL 262 CHECKS PASSED`

## Davranış
Migration mimarisi değişmedi:
- existing DB için pre-migration backup
- SQLite quick_check
- schema_version/schema_migrations
- ordered migration
- fail-closed backup failure
- newer schema downgrade refusal
- idempotent restart

Sadece compile blocker ve yanlış test sayacı düzeltildi.

## Windows testi
1. `7-SICHERHEITSTESTS.bat`
   Beklenen: `ALL 262 CHECKS PASSED`
2. `1-SETUP-ERSTELLEN.bat`
3. R68.1 mevcut DB ile programı aç.
4. Diagnose:
   `DATENBANK-SCHEMA · V1 AKTUELL`
5. `Backups\Migrationen` altında ilk geçişte bir PRE-MIGRATION backup.
