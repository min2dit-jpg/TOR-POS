# TOR POS R72.5 – Baseline & Avalonia Warning Cleanup

## Windows sonucu
R72.4 gerçek Windows testinde:

`ALL 300 CHECKS PASSED`

çıktı.

Yani artık SafetyTests başarısız değil. BAT'taki:
`Erwartet: ALL 298 CHECKS PASSED`

satırı eski ve yanlış kalmıştı.

## Test baseline düzeltmesi
Program.cs artık gerçek onaylı baseline'ı kendisi kontrol eder:

`ExpectedSafetyChecks = 300`

Runtime test sayısı 300 değilse test süreci exception ile başarısız olur.
Bu sayede BAT ve Program arasında tekrar sessiz sayı uyuşmazlığı oluşmaz.

BAT dosyasındaki hardcoded sayı kaldırıldı.
Başarı mesajı sadece test programının kendi baseline kontrolünden sonra çıkar.

## AVLN3001
LoginWindow, MainWindow ve SettingsWindow DI/AppWindowFactory ile oluşturulan
TopLevel pencerelerdir.

Bu nedenle public parameterless constructor eklenmedi.

Sadece AVLN3001 özel kodu:
`MSBuildWarningsAsMessages`

ile warning yerine build message seviyesine indirildi.

Diğer Avalonia warning/error kodları bastırılmadı.

## Manifest cleanup
Eski top-level manifest içinde kalmış:
`authentication.iterations = 180000`

değeri güncel gerçek hedef olan:
`600000`

olarak düzeltildi.

Legacy 180000 hash desteği rehash-on-login amacıyla korunur.

## Hedef
Windows:
- Build: 0 gerçek hata
- Safety: ALL 300 CHECKS PASSED
- AVLN3001: warning sayısına dahil edilmemeli

Schema V4, Angebot 40/50, KDF 600k, backup file-lock fix ve Cloud redirect fix
değişmeden korunur.
