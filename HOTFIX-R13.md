# TOR POS v0.7.33 R13 – Compile Hotfix

- R12 build failure fixed in `src/TorPos.App/Dialogs.cs`.
- `UniformGrid` belongs to `Avalonia.Controls.Primitives`; the missing namespace import was added.
- The R12 start/shortcut hotfix and R11 payment flow remain unchanged.
- This package should replace R12 for the next Windows build test.
